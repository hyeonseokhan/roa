export default function Page() {
  return (
      <div className="min-h-screen bg-black text-white flex items-center justify-center">
        <div className="w-full max-w-xl p-6 bg-gray-800 rounded-lg shadow-lg">
          <h1 className="text-2xl font-bold text-center mb-6">골드구매 계산기</h1>

          {/* 골드 가격과 원화 가격 */}
          <div className="grid grid-cols-2 gap-4 mb-6">
            <div>
              <label
                  htmlFor="gold-price"
                  className="block text-sm font-semibold text-amber-500 mb-2 flex items-center"
              >
                <img
                    src="https://cdn.rloa.gg/icons/gold.png"
                    alt="골드"
                    className="w-5 h-5 mr-2"
                />
                골드 가격
              </label>
              <input
                  type="text"
                  id="gold-price"
                  // value={goldPrice}
                  // onChange={(e) => setGoldPrice(parseFloat(e.target.value) || 0)}
                  className="w-full rounded-md bg-white px-3.5 py-2 text-gray-900 outline-none focus:ring-2 focus:ring-indigo-600"
              />
            </div>
            <div>
              <label
                  htmlFor="currency-price"
                  className="block text-sm font-semibold text-green-500 mb-2 flex items-center"
              >
                <img
                    src="https://cdn.rloa.gg/icons/silver.png"
                    alt="원화"
                    className="w-5 h-5 mr-2"
                />
                원화 가격
              </label>
              <input
                  type="text"
                  id="currency-price"
                  // value={currencyPrice}
                  // onChange={(e) => setCurrencyPrice(parseFloat(e.target.value) || 0)}
                  className="w-full rounded-md bg-white px-3.5 py-2 text-gray-900 outline-none focus:ring-2 focus:ring-indigo-600"
              />
            </div>
          </div>

          {/* 구매할 금액 입력 필드 */}
          <div className="mb-6">
            <label
                htmlFor="purchase-amount"
                className="block text-sm font-semibold text-white mb-2"
            >
              구매할 금액
            </label>
            <input
                type="text"
                id="purchase-amount"
                // value={purchaseAmount}
                // onChange={(e) => setPurchaseAmount(e.target.value)}
                placeholder="예: 500,000"
                className="w-full rounded-md bg-white px-3.5 py-3 text-xl text-gray-900 outline-none focus:ring-2 focus:ring-indigo-600"
            />
          </div>

          {/* 계산하기 버튼 */}
          <button
              className="w-full py-3 bg-amber-500 text-black font-bold rounded-lg hover:bg-amber-600"
          >
            계산하기
          </button>

          {/* 결과 출력 */}
          {/*{totalGold && (*/}
          {/*    <div className="mt-6 p-4 bg-gray-700 text-center rounded-lg">*/}
          {/*      <p className="text-xl font-bold">구매 가능한 골드: {totalGold}</p>*/}
          {/*    </div>*/}
          {/*)}*/}
        </div>
      </div>
  );
}
